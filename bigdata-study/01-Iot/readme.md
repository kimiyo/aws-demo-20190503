# AWS IoT Demonstration

#Set Python Environment
   python3 -m venv venv
   ls -altr
   source venv/bin/a
   source venv/bin/activate
   clear
   pip install AWSIoTPythonSDK
   pip install --upgrade pip
   pip freeze
   